apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    orgId: 1
    url: http://tailscale-prometheus:9090
    isDefault: true
    jsonData:
      httpMethod: GET
    editable: false
